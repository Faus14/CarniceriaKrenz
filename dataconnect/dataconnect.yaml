specVersion: "v1"
serviceId: "meat-palace"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "meat-palace-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
